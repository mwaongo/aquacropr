% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_eto.R
\name{write_eto}
\alias{write_eto}
\alias{write_et0}
\title{Write AquaCrop Reference Evapotranspiration File}
\usage{
write_eto(
  path = "weather/",
  stn = NULL,
  data = NULL,
  var_name = "et0",
  syear = NULL,
  eyear = NULL,
  eol = "windows",
  record_type = 1,
  first_day = 1,
  first_month = 1
)

write_et0(
  path = "weather/",
  stn = NULL,
  data = NULL,
  var_name = "et0",
  syear = NULL,
  eyear = NULL,
  eol = "windows",
  record_type = 1,
  first_day = 1,
  first_month = 1
)
}
\arguments{
\item{path}{Directory path where the output .ETo file will be written. Default = "weather/"}

\item{stn}{Station name or identifier for the weather station. Default = NULL (extracted from data if available)}

\item{data}{Data frame containing daily ETo data. Can have either:
\itemize{
\item Columns: station, year, month, day, et0 (legacy format), or
\item Columns: year, month, day, et0 (new format)
}}

\item{var_name}{Name of the ETo column in the data frame. Default = "et0"}

\item{syear}{Start year of the data period (extracted from data if not provided). Default = NULL}

\item{eyear}{End year of the data period (extracted from data if not provided). Default = NULL}

\item{eol}{End-of-line character style for the output file.
Options: "windows", "unix", "linux", or "macOS". Default = "windows"}

\item{record_type}{Type of temporal aggregation. Options: 1 = daily, 2 = 10-daily, 3 = monthly. Default = 1}

\item{first_day}{First day of record. For 10-daily: 1, 11, or 21; For monthly: 1. Default = 1}

\item{first_month}{First month of record (1-12). Default = 1}
}
\value{
Invisibly returns the full path to the created .ETo file. Creates a formatted .ETo file
in the specified directory with the format: <\if{html}{\out{<stn>}}>.ETo containing daily reference
evapotranspiration data as required by AquaCrop v7.0.
}
\description{
Write an AquaCrop v7.0 (August 2022) reference evapotranspiration (.ETo) file
containing daily ETo data for use in crop simulations.
}
\examples{
\dontrun{
  # Load example weather data
  data("weather")

  # Write ETo file with default column name
  write_eto(
    path = "weather/",
    stn = "Wakanda_Station",
    data = weather
  )

  # Write ETo file with custom column name
  write_eto(
    path = "weather/",
    stn = "Wakanda_Station",
    data = weather,
    var_name = "eto_calculated"
  )

  # Legacy format (backward compatibility)
  write_eto(data = weather_with_station, path = "climate/")
}

}
\seealso{
\code{\link{weather}} for example weather data,
\code{\link{write_plu}} for writing rainfall files,
\code{\link{write_tnx}} for writing temperature files
}
