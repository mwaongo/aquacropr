% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_irr.R
\name{write_irr}
\alias{write_irr}
\title{Write AquaCrop Irrigation File}
\usage{
write_irr(
  path = "MANAGEMENT/",
  irrigation_name,
  method,
  wet_surface,
  mode,
  irr_data,
  version = 7.1,
  eol = NULL,
  crop_length = NULL
)
}
\arguments{
\item{path}{Character. Directory path where the .IRR file will be created.
The directory is created automatically if it doesn't exist. Default: \code{"MANAGEMENT/"}.}

\item{irrigation_name}{Character. Name identifier for the irrigation scenario.
This name is used as the filename (e.g., \code{"drip-irrigation"} creates
\code{"drip-irrigation.IRR"}).}

\item{method}{Integer. Irrigation method:
\itemize{
\item 1 = Sprinkler irrigation
\item 2 = Surface irrigation: Basin
\item 3 = Surface irrigation: Border
\item 4 = Surface irrigation: Furrow
\item 5 = Drip irrigation
}}

\item{wet_surface}{Integer. Percentage of soil surface wetted by irrigation (0-100).
See Details for typical values by irrigation method.}

\item{mode}{Integer. Irrigation mode:
\itemize{
\item 1 = Specification of irrigation events
\item 2 = Generation of an irrigation schedule
\item 3 = Determination of net irrigation water requirement
}}

\item{irr_data}{Data.frame or numeric. Irrigation data. Structure depends on \code{mode}:
\itemize{
\item Mode 1: data.frame with columns \code{day}, \code{depth}, \code{ecw}. See \code{\link[=create_irr_events]{create_irr_events()}}
\item Mode 2: data.frame with columns \code{from_day}, \code{time_crit}, \code{depth_crit}, \code{ecw}
and attributes \code{time_crit_code}, \code{depth_crit_code}. See \code{\link[=create_irr_schedule]{create_irr_schedule()}}
\item Mode 3: Single numeric value representing allowable depletion of RAW (\%, 0-100)
}}

\item{version}{Numeric. AquaCrop version. Default: 7.1}

\item{eol}{Character. End-of-line character style. Options: "windows", "linux", or "macos".
If \code{NULL} (default), eol is auto-detected.}

\item{crop_length}{Integer. Optional. Length of crop cycle in days for validation.
If provided, warns about irrigation events beyond crop harvest.}
}
\value{
Invisibly returns the full path to the created .IRR file as a character string.
The main effect is writing the file to disk.
}
\description{
Creates an AquaCrop v7.0+ irrigation (.IRR) file that defines irrigation
management for simulation. The irrigation file specifies the irrigation method,
percentage of soil surface wetted, and irrigation scheduling approach.
}
\details{
\subsection{Typical Wetted Surface by Irrigation Method}{

The \code{wet_surface} parameter represents the percentage of soil surface wetted by irrigation.
Typical values by irrigation method:\tabular{ll}{
   Irrigation Method \tab Typical wet_surface (\%) \cr
   Sprinkler \tab 100 \cr
   Basin \tab 100 \cr
   Border \tab 100 \cr
   Furrow (every furrow, narrow bed) \tab 60-100 \cr
   Furrow (every furrow, wide bed) \tab 40-60 \cr
   Furrow (alternated furrows) \tab 30-50 \cr
   Drip/Micro irrigation \tab 15-40 \cr
   Subsurface drip \tab 0 \cr
}

}

\subsection{Irrigation Modes}{

\strong{Mode 1 - Specified Events}: Define exact irrigation dates, depths, and water quality.
Use \code{\link[=create_irr_events]{create_irr_events()}} to create properly formatted data.

\strong{Mode 2 - Generated Schedule}: Define rules that generate irrigation events automatically
based on time or depletion criteria. Use \code{\link[=create_irr_schedule]{create_irr_schedule()}} to create properly
formatted data with required attributes.

\strong{Mode 3 - Net Requirement}: Calculate total irrigation water needed to maintain
soil water above a specified threshold (\% RAW). Provide a single numeric value.
}
}
\examples{
\dontrun{
# Example 1: Fixed irrigation schedule (Mode 1)
events <- create_irr_events(
  day = c(10, 20, 30, 40, 50),
  depth = c(50, 50, 50, 50, 50),
  ecw = c(1.0, 1.0, 1.2, 1.4, 1.6)
)

write_irr(
  path = "MANAGEMENT/",
  irrigation_name = "ouaga-fixed",
  method = 5,        # Drip irrigation
  wet_surface = 30,  # 30\% surface wetted
  mode = 1,          # Specified events
  irr_data = events,
  crop_length = 120  # Optional: validate against 120-day cycle
)

# Example 2: Generated schedule with fixed interval (Mode 2)
schedule <- create_irr_schedule(
  from_day = c(1, 41, 116),
  time_crit = c(999, 7, 999),      # 7-day interval from day 41-115
  depth_crit = c(0, 40, 0),        # 40mm application depth
  ecw = c(0.4, 0.6, 0.8),
  time_crit_code = 1,              # Fixed interval
  depth_crit_code = 2              # Fixed depth
)

write_irr(
  path = "MANAGEMENT/",
  irrigation_name = "bobo-schedule",
  method = 1,         # Sprinkler
  wet_surface = 100,  # 100\% surface wetted
  mode = 2,           # Generated schedule
  irr_data = schedule
)

# Example 3: RAW-based automatic irrigation (Mode 2)
schedule_raw <- create_irr_schedule(
  from_day = c(1, 30, 60),
  time_crit = c(50, 40, 30),       # \% RAW depletion threshold
  depth_crit = c(0, 0, 5),         # Back to FC (+ 5mm on last period)
  ecw = c(0.5, 0.6, 0.7),
  time_crit_code = 3,              # \% RAW criterion
  depth_crit_code = 1              # Back to field capacity
)

write_irr(
  path = "MANAGEMENT/",
  irrigation_name = "auto-raw",
  method = 5,
  wet_surface = 30,
  mode = 2,
  irr_data = schedule_raw
)

# Example 4: Net irrigation requirement (Mode 3)
write_irr(
  path = "MANAGEMENT/",
  irrigation_name = "koudougou-netreq",
  method = 1,         # Sprinkler
  wet_surface = 100,
  mode = 3,           # Net requirement
  irr_data = 50       # Do not deplete below 50\% RAW
)
}

}
\seealso{
Other AquaCrop file writers: 
\code{\link{write_cal}()},
\code{\link{write_cli}()},
\code{\link{write_cro}()},
\code{\link{write_eto}()},
\code{\link{write_gwt}()},
\code{\link{write_man}()},
\code{\link{write_obs}()},
\code{\link{write_off}()},
\code{\link{write_plu}()},
\code{\link{write_prm}()},
\code{\link{write_swo}()},
\code{\link{write_tnx}()}
}
\concept{AquaCrop file writers}
