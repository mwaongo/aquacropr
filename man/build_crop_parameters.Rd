% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harvest_index.R
\name{build_crop_parameters}
\alias{build_crop_parameters}
\alias{crop_params}
\title{Build a Crop Parameter Set}
\usage{
build_crop_parameters(
  crop_cycle = 90,
  flowering_date = 35,
  plant_spacing = 0.5,
  row_spacing = 0.8,
  var_28 = 40,
  var_38 = 1,
  var_08 = 8,
  var_09 = 30
)

crop_params(
  crop_cycle = 90,
  flowering_date = 35,
  plant_spacing = 0.5,
  row_spacing = 0.8,
  var_28 = 40,
  var_38 = 1,
  var_08 = 8,
  var_09 = 30
)
}
\arguments{
\item{crop_cycle}{Integer. Total crop cycle length in days. Default = 90}

\item{flowering_date}{Integer. Days from sowing to flowering. Default = 35}

\item{plant_spacing}{Numeric. Distance between plants within rows (m). Default = 0.5}

\item{row_spacing}{Numeric. Distance between rows (m). Default = 0.8}

\item{var_28}{Numeric. Minimum canopy cover (\%) below which yield formation stops. Default = 40}

\item{var_38}{Numeric. Soil fertility stress coefficient (0-1). Default = 1 (no stress)}

\item{var_08}{Numeric. Base temperature (deg C) below which no growth occurs. Default = 8.0}

\item{var_09}{Numeric. Upper temperature (deg C) above which growth is optimal. Default = 30}
}
\value{
Named list of crop parameters suitable for passing to \code{\link{write_cro}}
}
\description{
Generate a complete set of crop parameters for AquaCrop by combining crop
phenology, harvest index timing, plant density, and other key parameters.
This is a convenience function for creating custom crop files.
}
\details{
This function combines outputs from:
\itemize{
\item \code{\link{calculate_crop_stages}}: Phenological stages (var_52-var_57)
\item \code{\link{calculate_harvest_index_timing}}: HI timing (var_60, var_64)
\item \code{\link{calculate_plant_density}}: Plant population (var_45)
\item Additional key parameters provided as arguments
}

All parameters are sorted by variable name (var_XX) for consistency with
AquaCrop file format.
}
\examples{
# Default maize parameters
build_crop_parameters()

# Custom wheat parameters
build_crop_parameters(
  crop_cycle = 150,
  flowering_date = 90,
  plant_spacing = 0.05,
  row_spacing = 0.20,
  var_08 = 0,
  var_09 = 26
)

# Use with write_cro
\dontrun{
params <- build_crop_parameters(
  crop_cycle = 120,
  flowering_date = 60
)

write_cro(
  crop_name = "custom-maize",
  params = params
)
}

}
\seealso{
\itemize{
\item \code{\link{calculate_crop_stages}} for phenology parameters
\item \code{\link{calculate_harvest_index_timing}} for HI parameters
\item \code{\link{calculate_plant_density}} for plant population
\item \code{\link{write_cro}} for writing crop files
}
}
