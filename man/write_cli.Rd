% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_cli.R
\name{write_cli}
\alias{write_cli}
\title{Write AquaCrop Climate File}
\usage{
write_cli(
  path = "weather/",
  stn = "station",
  eol = "windows",
  scenario = "hist",
  check_files = TRUE
)
}
\arguments{
\item{path}{Directory path where climate files are located and where .CLI will be written. Default = "weather/"}

\item{stn}{Station name or identifier. The .Tnx, .ETo, and .PLU files with this
station name must exist in the path. Default = "station"}

\item{eol}{End-of-line character style for the output file.
Options: "windows", "unix", "linux", or "macOS". Default = "windows"}

\item{scenario}{CO2 scenario to use. Options: "hist" (historical/Mauna Loa),
"rcp26", "rcp45", "rcp60", "rcp85", "ssp119", "ssp126", "ssp245", "ssp370", "ssp585".
Default = "hist"}

\item{check_files}{Logical. If TRUE, checks that all three required climate files (.Tnx, .ETo, .PLU)
exist before writing .CLI file. Default = TRUE}
}
\value{
Invisibly returns the full path to the created .CLI file. The function also copies
the appropriate CO2 file to the specified path if it doesn't already exist there.
}
\description{
Write an AquaCrop v7.0 (August 2022) climate (.CLI) file that references the
temperature, ETo, and rainfall files, along with the appropriate CO2 concentration file
for the simulation scenario.
}
\details{
The .CLI file is the main climate file that references:
\itemize{
\item Temperature file (.Tnx)
\item Reference evapotranspiration file (.ETo)
\item Rainfall file (.PLU)
\item CO2 concentration file (.CO2)
}

\strong{Important}: By default, the function validates that all three climate files (.Tnx, .ETo, .PLU)
exist before creating the .CLI file. Set \code{check_files = FALSE} to skip this validation
(not recommended unless you're sure the files will be created later).
\subsection{CO2 Scenarios:}{
\itemize{
\item \strong{hist}: Historical data from Mauna Loa Observatory (MaunaLoa.CO2)
\item \strong{RCP scenarios}: Representative Concentration Pathways
\itemize{
\item rcp26: RCP 2.6 (low emissions)
\item rcp45: RCP 4.5 (intermediate emissions)
\item rcp60: RCP 6.0 (intermediate-high emissions)
\item rcp85: RCP 8.5 (high emissions)
}
\item \strong{SSP scenarios}: Shared Socioeconomic Pathways
\itemize{
\item ssp119: SSP1-1.9 (very low emissions)
\item ssp126: SSP1-2.6 (low emissions)
\item ssp245: SSP2-4.5 (intermediate emissions)
\item ssp370: SSP3-7.0 (high emissions)
\item ssp585: SSP5-8.5 (very high emissions)
}
}
}
}
\examples{
\dontrun{
# First, create the required climate files
data("weather")
write_plu(data = weather, stn = "Wakanda", path = "weather/")
write_eto(data = weather, stn = "Wakanda", path = "weather/")
write_tnx(data = weather, stn = "Wakanda", path = "weather/")

# Then write CLI file with historical CO2
write_cli(
  path = "weather/",
  stn = "Wakanda",
  scenario = "hist"
)

# Write CLI file with RCP 4.5 scenario
write_cli(
  path = "weather/",
  stn = "Wakanda",
  scenario = "rcp45"
)

# Skip validation (not recommended)
write_cli(
  path = "weather/",
  stn = "Wakanda",
  scenario = "hist",
  check_files = FALSE
)
}

}
\seealso{
\code{\link{write_plu}} for writing rainfall files,
\code{\link{write_eto}} for writing ETo files,
\code{\link{write_tnx}} for writing temperature files
}
