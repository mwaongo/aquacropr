% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_irr.R
\name{create_irr_events}
\alias{create_irr_events}
\title{Create Irrigation Events Data}
\usage{
create_irr_events(day, depth, ecw, crop_length = NULL)
}
\arguments{
\item{day}{Integer vector. Day after sowing/planting for each irrigation event (1-366)}

\item{depth}{Numeric vector. Net irrigation application depth in mm for each event.
This is the net amount only - do not include conveyance losses.}

\item{ecw}{Numeric vector. Electrical conductivity of irrigation water in dS/m for each event}

\item{crop_length}{Integer. Optional. Length of crop cycle in days for validation.
If provided, warns about irrigation events beyond crop harvest.}
}
\value{
A tibble with columns \code{day}, \code{depth}, \code{ecw}, ready for use with \code{\link[=write_irr]{write_irr()}}
}
\description{
Helper function to create properly formatted irrigation events data
for use with \code{\link[=write_irr]{write_irr()}} in mode 1 (specification of irrigation events).
}
\examples{
\dontrun{
# Fixed irrigation schedule for dry season tomato
events <- create_irr_events(
  day = c(10, 20, 30, 40, 50, 60, 70, 80),
  depth = c(40, 40, 50, 50, 50, 40, 40, 30),
  ecw = rep(0.5, 8),
  crop_length = 120  # Optional: validate against 120-day cycle
)

# Variable water quality scenario
events_variable <- create_irr_events(
  day = c(15, 30, 45, 60),
  depth = c(50, 50, 50, 50),
  ecw = c(0.5, 0.8, 1.2, 1.5)  # Increasing salinity
)
}

}
