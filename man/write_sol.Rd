% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_sol.R
\name{write_sol}
\alias{write_sol}
\alias{write_soil}
\title{Write AquaCrop Soil Profile File (.SOL)}
\usage{
write_sol(
  site_name = "default-site",
  path = "SOIL/",
  cn = 46,
  rew = 5,
  texture = c("loam", "silt loam"),
  thickness = c(0.5, 1),
  eol = "windows",
  write_sw0 = TRUE,
  initial_water = "WP",
  salinity = "none",
  initial_cc = -9,
  initial_biomass = 0,
  initial_root_depth = -9,
  bund_water = 0,
  bund_ec = 0,
  swo_layers = NULL
)

write_soil(
  site_name = "default-site",
  path = "SOIL/",
  cn = 46,
  rew = 5,
  texture = c("loam", "silt loam"),
  thickness = c(0.5, 1),
  eol = "windows",
  write_sw0 = TRUE,
  initial_water = "WP",
  salinity = "none",
  initial_cc = -9,
  initial_biomass = 0,
  initial_root_depth = -9,
  bund_water = 0,
  bund_ec = 0,
  swo_layers = NULL
)
}
\arguments{
\item{site_name}{Name of the soil profile (used in the filename).
Default: \code{"default-site"}.}

\item{path}{Directory where the \code{.SOL} file will be written.
Default: \code{"soil/"}.}

\item{cn}{Curve number for runoff estimation (0–100). Default: \code{46}.}

\item{rew}{Readily evaporable water (mm) from the top soil layer. Default: \code{5}.}

\item{texture}{Character vector of soil texture names (USDA classes).
Must match entries in the \code{SoilWater} dataset.}

\item{thickness}{Numeric vector of horizon thicknesses (m).
Must have the same length as \code{texture}.}

\item{eol}{End-of-line style. One of \code{"windows"}, \code{"unix"}, \code{"linux"}, \code{"macOS"}.
Default: \code{"windows"}.}

\item{write_sw0}{Logical; if \code{TRUE}, also write the corresponding \code{.SW0}
file by calling \code{\link[=write_swo]{write_swo()}}. Default: \code{TRUE}.}

\item{initial_water}{Character string or numeric. Specifies initial water content for all layers.
Options:
\itemize{
\item \strong{"WP"} or \strong{"wilting_point"}: Sets to wilting point based on soil texture
\item \strong{"FC"} or \strong{"field_capacity"}: Sets to field capacity (1.5 × WP)
\item \strong{"SAT"} or \strong{"saturation"}: Sets to saturation (2.5 × WP)
\item \strong{Numeric value}: Sets all layers to specified value (0-100\%)
}
Only used if layers parameter is not provided. Default = "WP"}

\item{salinity}{Character string or numeric specifying soil salinity level for all layers.
Options: "none", "very slight", "slight", "moderate", "strong", "very strong",
or numeric ECe value (0-100 dS/m). Only used if layers parameter is not provided.
Default = "none"}

\item{initial_cc}{Numeric. Initial canopy cover that can be reached without water stress (\%).
Use -9.00 for automatic calculation by AquaCrop. Default = -9.00}

\item{initial_biomass}{Numeric. Biomass produced before simulation start (ton/ha). Default = 0.000}

\item{initial_root_depth}{Numeric. Initial effective rooting depth without water stress (m).
Use -9.00 for automatic calculation. Default = -9.00}

\item{bund_water}{Numeric. Water layer stored between soil bunds if present (mm). Default = 0.0}

\item{bund_ec}{Numeric. Electrical conductivity of water between bunds (dS/m). Default = 0.00}

\item{swo_layers}{Optional \code{data.frame} to pass as \code{layers} to \code{\link[=write_swo]{write_swo()}}.
If \code{NULL} (default), \code{\link[=write_swo]{write_swo()}} will construct layers from \code{texture}
and \code{thickness}.}
}
\value{
Invisibly returns the path to the written \code{.SOL} file.
}
\description{
Writes an AquaCrop v7.0 (August 2022) soil profile file (\code{.SOL}) containing
hydraulic and physical properties for each soil horizon.
Optionally, when \code{write_sw0 = TRUE}, also writes the matching initial
soil-water content file (\code{.SW0}) by calling \code{\link[=write_swo]{write_swo()}}.
}
\details{
\subsection{File content}{

The \code{.SOL} file defines hydraulic and physical parameters for each soil
horizon, including:
\itemize{
\item \strong{SAT}: Saturation (vol \%)
\item \strong{FC}: Field capacity (vol \%)
\item \strong{WP}: Wilting point (vol \%)
\item \strong{Ksat}: Saturated hydraulic conductivity (mm day⁻¹)
\item \strong{Penetrability, Gravel, CRa, CRb}: Additional physical characteristics
}

The number of horizons is inferred from the lengths of \code{texture} and \code{thickness}.
}

\subsection{Writing the \code{.SW0} file}{

When \code{write_sw0 = TRUE}, \code{\link[=write_swo]{write_swo()}} is called using the same \code{path},
\code{site_name} (as \code{soil_name}), \code{texture}, \code{thickness} (as \code{layer_thickness}),
and \code{eol}. The following arguments are passed through:
\code{initial_water}, \code{salinity}, \code{initial_cc}, \code{initial_biomass},
\code{initial_root_depth}, \code{bund_water}, \code{bund_ec}, and \code{swo_layers} (as \code{layers}).
}
}
\examples{
\dontrun{
# Basic soil profile (single horizon)
write_sol(
  path = "soil/",
  site_name = "field1",
  texture = "loam",
  thickness = 1.0
)

# Two horizons with automatic SW0 generation (field capacity)
write_sol(
  path = "soil/",
  site_name = "layered",
  cn = 65,
  rew = 7,
  texture = c("sandy loam", "clay loam"),
  thickness = c(0.4, 0.6),
  initial_water = "FC",
  salinity = "none"
)

# Write only the .SOL file (no SW0)
write_sol(
  site_name = "soil_only",
  texture = "loam",
  thickness = 1.0,
  write_sw0 = FALSE
)
}

}
\seealso{
\link{SoilWater} for hydraulic property data,
\code{\link[=write_swo]{write_swo()}} for initial water content files (.SW0),
\code{\link[=write_cro]{write_cro()}} for crop files,
\code{\link[=write_man]{write_man()}} for management files.
}
